# Pokémon Hunter fork — for Claude

Work happens on branch `hunter-dev`. Design docs and PROJECT MEMORY live in
the `PradyXiii/pokemon-hunter` repo — read its `MEMORY.md` index first;
`memory/engine.md` lists the flags/vars/trainer IDs this fork has already
claimed and the code touchpoints. Never reuse claimed resources. Only 5
trainer IDs (859-863) remain free — flag refactor needed before more.
Build: `make -j4`. Never commit the built ROM.
